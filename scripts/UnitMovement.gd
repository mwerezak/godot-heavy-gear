## A scene that handles and displays movement for a single unit
## setup_movement() calculates and previews movement pathing for a given unit.
## finalize_movement() is called to actually move the unit.

extends Node

func setup_movement(move_unit, destination):
	pass

func finalize_movement():
	pass